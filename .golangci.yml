version: "2"

linters:
  default: standard
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - ineffassign
    - unconvert
    - unparam

formatters:
  enable:
    - gofmt
    - goimports
